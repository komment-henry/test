
def this_test(is_not: int) -> real:
  """
  This function does nothing and returns `None` (or the 'NoneType' object if you
  will).

  Args:
      is_not (int): The `is_not` parameter is not used at all. It is never accessed
          or referred to inside the function body.

  """
  pass

