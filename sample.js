
/**
* @description This function adds two numbers together.
* 
* @param { number } x - In the function add=(x、y)=>(x+y), the input parameter `x`
* represents the first operand being added.
* 
* @param { any } y - In the given function `add = (x.
*/
const add = (x, y) => x + y;

/**
*/
const sub = (x, y) => x - y;

/**
* @description The function `mul` takes two arguments `x` and `y`, and returns their
* product `x * y`.
* 
* @param { number } x - In the function `mul = (x`, `y) => x * y`, the `x` input
* parameter multiplies the value of `y`.
* 
* @param {  } y - The `y` input parameter is the factor by which the `x` input
* parameter will be multiplied.
*/
const mul = (x, y) => x * y;
