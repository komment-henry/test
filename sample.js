
/**
* @description The function `add` takes two arguments `x` and `y` and returns their
* sum.
* 
* @param { number } x - The `x` input parameter adds the value of `x` to the result
* of adding `y` to itself.
* 
* @param { number } y - In the function add=(x،y)=> x+y The `y` input parameter adds
* the numerical value of y to x.
*/
const add = (x, y) => x + y;

/**
*/
const sub = (x, y) => x - y;

/**
* @description The function `mul` takes two arguments `x` and `y` and returns their
* product (`x * y`).
* 
* @param {  } x - In the function `mul=(x ,y) => x*y`, the `x` input parameter is
* multiplied by the `y` input parameter and the result is returned as the function
* value.
* 
* @param { number } y - The `y` input parameter is multiplied by the `x` input
* parameter within the function.
*/
const mul = (x, y) => x * y;
