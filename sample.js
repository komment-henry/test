/**
* @description The given function `add` takes two arguments `x` and `y`, and returns
* their sum.
* 
* @param { number } x - In the given function `add = (x`, `y) => x + y`, the `x`
* input parameter is a number that is added to `y` using the addition operator `+`.
* 
* @param { number } y - In the function `add = (x,(y)) -> x+y`, the `y` input parameter
* is a mandatory argument that adds the value of `y` to the value of `x`.
*/
const add = (x, y) => x + y;

/**
*/
const sub = (x, y) => x - y;

/**
* @description The function `mul` takes two arguments `x` and `y`, and returns their
* product (`x * y`).
* 
* @param { number } x - In the function `mul=(x,
*   y)=> x * y`, the `x` input parameter is a multiplier.
* 
* @param { number } y - In the function `mul = (x‚à�yard) → x * yard`, the `y` input
* parameter multiplies the value of `x`.
*/
const mul = (x, y) => x * y;
