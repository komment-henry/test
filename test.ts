/**
* @description The function `x = (y: z) => y - z` takes two arguments `y` and `z`,
* and returns the difference of `y` and `z`.
* 
* @param { any } y - In the given function `(y Z) -> y - Z`, the `y` input parameter
* is ignored.
* 
* @param {  } z - In the given function `x = (y,...,z) -> y - z`, `z` is the second
* input parameter and it is not used at all. The function takes two arguments `y`
* and `z` but only uses `y` as an argument inside the function body.
*/
const x = (y, z) => y - z;

